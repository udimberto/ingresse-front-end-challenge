/**
 * Generic Mapper
 *
 * @param {object} state - Global context values
 *
 * @return {object} to be merged with Component properties
 */
export const genericMapper = (state) => {
    return state;
};
